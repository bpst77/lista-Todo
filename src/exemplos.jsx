/* 
  exemplos

    container simples-
    <div>
      <h1>Get started</h1>
      <p>
      Não edite <code>src/App.jsx</code> and save to test <code>HMR</code>
      </p>
    </div>

    atributos em elementos-
    <button
      type="button"
      className="counter"
      onClick={() => setCount((count) => count + 1)}
    >
      Count is {count}
    </button>
*/